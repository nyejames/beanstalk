[backends.html]
mode = "failure"
warnings = "forbid"
diagnostic_codes = ["BST-RULE-0043"]
message_contains = [
    "'DisplayText' is not a valid trait name",
    "Trait names must use all-caps identifiers",
]
