entry = "."

[backends.html]
mode = "success"
warnings = "forbid"
rendered_output_contains = ["option_postfix_propagation_success", "Hello Priya", "missing", "Fallible Priya", "fallible missing"]

[[backends.html.artifact_assertions]]
path = "index.html"
kind = "html"
must_contain = [".tag === \"some\"", ".tag === \"none\""]
