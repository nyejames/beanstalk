[backends.html]
mode = "success"
warnings = "forbid"
rendered_output_contains_in_order = ["template_loop_output_then_continue_runtime", "before 1", "before 2"]
rendered_output_contains_exactly_once = ["template_loop_output_then_continue_runtime", "before 1", "before 2"]
rendered_output_not_contains = ["after 1", "after 2"]

[[backends.html.artifact_assertions]]
path = "index.html"
kind = "html"
must_contain = ["template_loop_output_then_continue_runtime", "output-then-continue"]
