[backends.html]
mode = "failure"
warnings = "forbid"
diagnostic_codes = ["BST-TYPE-0003"]
message_contains = ["Operator `+` is not available for generic parameters `A` and `B`. Beanstalk operators are compiler-owned"]
