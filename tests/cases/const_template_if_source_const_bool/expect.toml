[backends.html]
mode = "success"
warnings = "forbid"

[[backends.html.artifact_assertions]]
path = "index.html"
kind = "html"
must_contain = ["Same-file source const visible"]
must_not_contain = ["Same-file source const hidden"]
