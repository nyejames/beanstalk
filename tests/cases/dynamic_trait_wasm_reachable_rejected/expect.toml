entry = "."

[backends.html]
mode = "success"
warnings = "forbid"
rendered_output_contains = ["wasm dynamic=wasm"]

[backends.html_wasm]
mode = "failure"
warnings = "forbid"
diagnostic_codes = ["BST-RULE-0064"]
message_contains = ["Wasm", "dynamic trait value construction"]
