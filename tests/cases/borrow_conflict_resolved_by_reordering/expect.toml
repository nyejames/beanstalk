entry = "."

[backends.html]
mode = "success"
warnings = "forbid"
rendered_output_contains = [
    "borrow_conflict_resolved_by_reordering shared-alias=42",
    "borrow_conflict_resolved_by_reordering mutable-alias=7",
]
