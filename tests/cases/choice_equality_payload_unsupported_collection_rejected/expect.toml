entry = "."

[backends.html]
mode = "failure"
warnings = "forbid"
error_type = "Type"
message_contains = ["Choice payload equality is not supported because field 'items' has type '{Int}', which does not support equality"]
