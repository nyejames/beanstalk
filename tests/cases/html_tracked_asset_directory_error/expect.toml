entry = "."

[backends.html]
mode = "failure"
warnings = "forbid"
error_type = "Rule"
message_contains = ["tracked asset", "file-only"]

[backends.html_wasm]
mode = "failure"
warnings = "forbid"
error_type = "Rule"
message_contains = ["tracked asset", "file-only"]
