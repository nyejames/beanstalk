[backends.html]
mode = "failure"
warnings = "forbid"
diagnostic_codes = ["BST-RULE-0042"]
message_contains = ["Value-producing blocks are only valid at declaration, assignment, return, multi-bind, catch, or `then` receiving sites."]
