[backends.html]
mode = "failure"
warnings = "forbid"
diagnostic_codes = ["BST-DEFERRED-0001"]
message_contains = ["Receiver methods on generic types are not supported", "Use a free function instead"]
