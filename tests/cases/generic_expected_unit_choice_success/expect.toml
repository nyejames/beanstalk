[backends.html]
mode = "success"
warnings = "forbid"
golden_mode = "normalized"
rendered_output_contains = ["expected unit choice generic"]

[backends.html_wasm]
mode = "failure"
warnings = "forbid"
error_type = "wasm_generation"
message_contains = ["Wasm validation failed"]
