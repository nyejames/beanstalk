[backends.html]
mode = "failure"
warnings = "forbid"
diagnostic_codes = ["BST-RULE-0047"]
message_contains = [
    "file-local trait evidence",
    "generic-bound receiver calls do not support yet",
]
