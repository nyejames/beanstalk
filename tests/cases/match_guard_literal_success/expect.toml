entry = "."

[backends.html]
mode = "success"
warnings = "exact"
warning_count = 1
rendered_output_contains = ["match_guard_literal_success result=allowed"]

[[backends.html.artifact_assertions]]
path = "index.html"
kind = "html"
must_contain = ["&&", "=== 2"]
must_not_contain = ["switch (__bb"]
