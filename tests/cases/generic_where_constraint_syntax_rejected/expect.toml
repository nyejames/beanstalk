[backends.html]
mode = "failure"
warnings = "forbid"
diagnostic_codes = ["BST-DEFERRED-0001"]
message_contains = [
    "`where` syntax is not part of Beanstalk generic constraints.",
    "type A is TRAIT",
]
