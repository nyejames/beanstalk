[backends.html]
mode = "failure"
warnings = "forbid"
diagnostic_codes = ["BST-TYPE-0003"]
message_contains = ["Generic parameter 'T' cannot be used with arithmetic operators before trait bounds are supported"]
