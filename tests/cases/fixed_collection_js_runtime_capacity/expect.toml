[backends.html]
mode = "success"
warnings = "forbid"
rendered_output_contains = [
    "fixed_collection_js_runtime lengths=0,1,2,1,2 removed=1 values=2,3"
]

[[backends.html.artifact_assertions]]
path = "index.html"
kind = "html"
must_contain = [
    "__bs_fixed_collection([], 2)",
    "__bst_kind: \"fixed_collection\"",
    "fixedCapacity: fixedCapacity",
    "Fixed collection capacity exceeded",
]
must_not_contain = [
    "__bs_result_propagate(__bs_collection_length(",
]
