[backends.html]
mode = "success"
warnings = "forbid"
rendered_output_contains = ["value catch inline recovery", "0", "fallback", "guest"]

[[backends.html.artifact_assertions]]
path = "index.html"
kind = "html"
must_contain = ["value catch inline recovery", "0", "fallback", "guest"]
