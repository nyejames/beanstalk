[backends.html]
mode = "success"
warnings = "forbid"
rendered_output_contains = [
    "template_loop_nested_if_continue_runtime",
    "before 1;",
    "before 2;",
    "after 2;",
]
rendered_output_not_contains = ["after 1;"]
