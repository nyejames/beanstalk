-- Test variable mutation parsing

-- Declare a mutable variable
x ~= 5

-- Simple mutation
x = 10

-- Compound assignment operations
x += 5
x -= 2
x *= 3
x /= 2

-- Test with different types
name ~= "hello"
name = "world"

-- Test error cases (these should fail)
-- immutable_var = 42  -- This should error: cannot mutate immutable variable
-- x ~= 20             -- This should error: variable already declared