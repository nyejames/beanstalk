-- Collection literals still need an end-to-end integration smoke test.
-- Computed-place collection index helpers are covered directly in JS backend unit tests.

nums = {10, 20, 30}
io("collection literal smoke")
