[backends.html]
mode = "failure"
warnings = "forbid"
diagnostic_codes = ["BST-RULE-0054"]
message_contains = ["cannot use `~` on a fresh or computed value for parameter 'value'", "Remove `~` and pass the value directly"]
