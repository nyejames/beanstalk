entry = "."

[backends.html]
mode = "success"
warnings = "forbid"
rendered_output_contains = ["generic builtin evidence: 42|true|Z"]

[backends.html_wasm]
mode = "failure"
warnings = "forbid"
diagnostic_codes = ["BST-RULE-0064"]
message_contains = ["html_wasm", "runtime casts"]
