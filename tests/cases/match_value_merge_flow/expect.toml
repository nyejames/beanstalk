entry = "."

[backends.html]
mode = "success"
warnings = "forbid"
rendered_output_contains = ["match_value_merge_flow result=25"]

[[backends.html.artifact_assertions]]
path = "index.html"
kind = "html"
must_contain = ["if (", "=== 1", "=== 2"]
must_not_contain = ["switch (__bb"]
