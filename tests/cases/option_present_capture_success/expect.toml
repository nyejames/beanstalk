entry = "."

[backends.html]
mode = "success"
warnings = "forbid"
rendered_output_contains = ["option_present_capture_success result=guard=42"]

[[backends.html.artifact_assertions]]
path = "index.html"
kind = "html"
must_contain = [".tag === \"some\""]
