[backends.html]
mode = "success"
warnings = "forbid"
rendered_output_contains = ["0", "then tail after value production is unreachable"]

[[backends.html.artifact_assertions]]
path = "index.html"
kind = "html"
must_contain = ["then tail after value production is unreachable"]
