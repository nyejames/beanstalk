-- Basic Maths Tests
#export test1 || -> int:
    return 3 + 2 * 2 -- 7
zz
#export test2 || -> int:
    return 3 + 3 -- 6
zz
#export test3 || -> int:
    return 2 * 2 + 1 -- 5
zz
#export test4 || -> int:
    return 2 ^ 2 -- 4
zz
#export test5 || -> int:
    return 3 - 0 -- 3
zz
#export test6 || -> int:
    return 8 / 2 / 2 -- 2
zz