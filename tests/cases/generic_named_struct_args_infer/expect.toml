[backends.html]
mode = "success"
warnings = "forbid"
golden_mode = "normalized"
rendered_output_contains = ["named struct generic inference"]

[backends.html_wasm]
mode = "failure"
warnings = "forbid"
error_type = "lir_transformation"
message_contains = ["Wasm lowering does not yet support this expression construct"]
