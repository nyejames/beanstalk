entry = "."

[backends.html]
mode = "success"
warnings = "forbid"
rendered_output_contains = ["html_wasm_collection_runtime_coverage len=3 picked=99"]

[backends.html_wasm]
mode = "failure"
warnings = "forbid"
error_type = "LirTransformation"
message_contains = ["Wasm backend does not yet support host function '__bs_collection_length'"]
