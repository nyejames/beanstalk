[backends.html]
mode = "success"
warnings = "forbid"

[[backends.html.artifact_assertions]]
path = "index.html"
kind = "html"
must_contain = [".tag === 0", ".tag === 1", ".tag === 2"]
must_not_contain = ["switch (", "while (true)"]
