entry = "."

[backends.html]
mode = "failure"
warnings = "forbid"
error_type = "Type"
message_contains = ["Choice payload equality is not supported because field 'handle' has type 'External(", "which does not support equality"]
