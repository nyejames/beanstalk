entry = "."

[backends.html]
mode = "failure"
warnings = "forbid"
diagnostic_codes = ["BST-RULE-0041"]
message_contains = ["File-local extension receiver methods are not importable from another file"]
