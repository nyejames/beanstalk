[backends.html]
mode = "failure"
warnings = "forbid"
diagnostic_codes = ["BST-RULE-0054"]

message_contains = ["requires explicit mutable access for parameter 'value'", "Prefix the existing mutable place with `~`"]

[[backends.html.diagnostic_assertions]]
code = "BST-RULE-0054"
reason = "invalid_call_shape.mutable_access_required"
