entry = "."

[backends.html]
mode = "success"
warnings = "forbid"
rendered_output_contains = ["structured_match_converge result=many"]

[[backends.html.artifact_assertions]]
path = "index.html"
kind = "html"
must_contain = ["const __match_"]
must_not_contain = ["while (true)", "switch (__bb"]
