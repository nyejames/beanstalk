entry = "."

[backends.html]
mode = "success"
warnings = "forbid"
rendered_output_contains = ["option_match_patterns_success name=ana user=missing score=medium"]

[[backends.html.artifact_assertions]]
path = "index.html"
kind = "html"
must_contain = [".tag === \"none\"", ".tag === \"some\""]
