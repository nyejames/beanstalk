
## Design contract

Existing values use shared, read-only access by default. Independent copies
require explicit `copy`. Mutation requires explicit exclusive access to an
existing mutable place.

These are source-semantic rules. Backends may use different scalar, handle,
pointer or GC representations as long as they preserve the same observable
behaviour.

## Main compiler paths

- `src/compiler_frontend/value_mode.rs`
- `src/compiler_frontend/ast/expressions/`
- `src/compiler_frontend/ast/statements/`
- `src/compiler_frontend/ast/field_access/`
- `src/compiler_frontend/hir/`

This is a navigation map, not a status declaration.

## Vocabulary

[table:
    [th:
        [: **Term**]
        [: **Definition**]
    ]

    [td:
        [: Value]
        [: A Beanstalk language value. Not necessarily heap allocated.]
    ]

    [td:
        [: Place]
        [: Existing storage that can be read or mutated.]
    ]

    [td:
        [: Shared access]
        [: Read-only access to one or more underlying roots.]
    ]

    [td:
        [: Alias binding]
        [: A binding that observes existing storage rather than creating an independent copy.]
    ]

    [td:
        [: Copy]
        [: A new independent value created through explicit copy semantics.]
    ]

    [td:
        [: Exclusive access]
        [: Mutation-capable access to one existing place.]
    ]

    [td:
        [: Move]
        [: Compiler-inferred ownership transfer at a legal consumption point.]
    ]

    [td:
        [: Fresh value]
        [: A newly produced literal, template, constructor result or computed aggregate.]
    ]
]

"Reference" is useful explanatory vocabulary but not a first-class source type.

## Shared access

Reads of existing values are shared. Ordinary immutable arguments are shared.
Multiple shared reads may coexist. Shared access is read-only, doesn't imply
ownership and can prevent mutation while live. Scalar values may be
represented directly by a backend.

[codeblock, $code("bst"):
    first = load_items()
    second = first

    inspect(first)
    inspect(second)
]

`second` doesn't imply a clone. It's a shared read-only alias of the same
source value.

## Alias bindings

[codeblock, $code("bst"):
    alias = source
]

An alias binding normally observes the same source-semantic storage or root.
It doesn't create an independent value. It can participate in use-after-move
and mutation-conflict analysis. An ownership-aware backend may optimise an
alias into storage transfer when no later source use exists, but it must still
behave as though source aliasing rules were respected.

Borrow-checker slot and alias states are analysis concepts, not source-visible
runtime types.

## Explicit copies

[codeblock, $code("bst"):
    snapshot = copy source
]

`copy` creates independent value semantics. All source types use explicit copy
when an independent duplicate is required. Expressions can construct new
values from shared inputs: constructing a result doesn't implicitly copy the
inputs. Template interpolation reads inputs and produces a new output string.
Map insertion and aggregate storage follow ordinary move and copy rules.

## Mutable bindings

[codeblock, $code("bst"):
    count ~= 0
    count = 1
]

`~` on the declaration marks the binding as mutation-capable. Reassignment
still uses `=`. Mutable binding syntax is different from call-site exclusive
access. Mutability is an access property, not separate semantic type identity.

## Exclusive access with ~

[codeblock, $code("bst"):
    update(~items)
    ~items.push(value)!
]

The source form is `~place`. The place must already exist, and the underlying
binding must permit mutation. Exactly one exclusive access may be active at a
time. Shared access conflicts with active exclusive mutation where their roots
overlap. `~` isn't an explicit move. The compiler may later realise an
eligible operation as ownership transfer.

## Fresh values and hidden locals

[codeblock, $code("bst"):
    update({"fresh"})
    update(Vector2(x = 1.0, y = 2.0))
    update([: generated])
]

These forms are valid. Source `~` on a fresh value isn't:

[codeblock, $code("bst"):
    update(~{"fresh"})   -- invalid
    update(~Vector2(x = 1.0, y = 2.0))   -- invalid
]

Fresh values don't need source `~`. A mutable parameter may accept them. HIR
materialises a hidden local when borrow analysis needs a place. The hidden
local isn't source-visible lifetime syntax. There is no special
temporary-reference node category after materialisation.

## Calls and receiver methods

- A shared parameter from an existing place needs no `~`.
- A mutable parameter from an existing place requires `~place`.
- A mutable parameter from a fresh value uses the plain value.
- A mutable receiver uses `~value.method(...)`.
- An immutable receiver uses `value.method(...)`.
- Temporaries can't be used as mutable receivers unless the source form is
  accepted as a fresh argument by the call boundary.
- Source receiver mutability remains separate from ownership.

## Collections, maps and stored values

Aggregates own stored values under the ownership model. Insertion may borrow,
copy or consume according to the accepted access and ownership contract.
Independent duplication requires `copy`. Mutating collection and map methods
require exclusive receiver access.

Map `get` returns shared access into stored data. The same map can't be
mutated while that shared result remains live. Map `remove` returns the
removed owned value. Map `set` doesn't silently duplicate keys or values
outside ordinary copy and move rules.

## No explicit reference, move or lifetime syntax

Beanstalk has no source-level:

- `&` or `&mut` reference type constructors.
- lifetime annotations or lifetime parameters.
- temporary-reference syntax.
- explicit move keyword or operator.
- separate borrowed and owned function signatures.

These are permanent design boundaries, not merely unimplemented features.

## No shadowing

One visible name maps to one binding. This keeps alias roots, future-use
reasoning and diagnostics easier to reason about. The full language
no-shadowing rule remains in `docs/language-overview.md`.

## Common mistakes

- Treating `x = y` as an implicit clone.
- Treating `~` as a move marker.
- Treating `~T` as a distinct semantic type.
- Requiring `~` on a fresh value.
- Inventing source-visible temporary references.
- Allowing mutation while a shared alias is live.
- Making backend scalar representation define source copy semantics.
- Treating explicit copy as merely a backend hint.

## Related reading

- @../ (Memory management): concise memory-model contract
- @../borrow-validation (Borrow validation): how shared and exclusive access are checked
- @../ownership-and-drops (Ownership and drops): inferred transfer after access validation
- @../../compiler-design/ast/expressions-statements-and-terminality
  (Expressions, statements and terminality): AST source access ownership
- @../../compiler-design/stages/hir-generation
  (HIR generation): hidden locals and explicit places
- @https://github.com/nyejames/beanstalk/blob/main/docs/language-overview.md (Language overview): full source syntax
