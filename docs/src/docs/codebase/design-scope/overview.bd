# Design scope

Accepted language and compiler design boundaries.

## Authority

`docs/language-overview.md` owns the complete compiler-facing language-scope
list. This page is a compact summary of the main boundaries. The progress
matrix reports current implementation and diagnostic coverage.

## Central bias

- Keep the source language small, explicit and statically inspectable.
- Compiler complexity is acceptable when it makes source code simpler.
- Prefer constrained built-in mechanisms over general metaprogramming.
- Prefer static resolution over runtime dynamism.
- Keep backend optimisations hidden behind stable source semantics.
- Avoid features that need pervasive solver-heavy reasoning.

## Deferred versus outside scope

- **Deferred implementation** fits accepted design but has not landed or is
  incomplete.
- **Outside scope** conflicts with the intended language model and needs an
  explicit design change before implementation.

The progress matrix reports whether a deferred surface is implemented today.
This page explains whether the surface belongs in the language at all.

## Preferred constrained mechanisms

| Need | Preferred mechanism |
| ---- | ------------------- |
| Compile-time generation | Templates, constants and builder directives |
| Static reuse | Generic functions and explicit trait bounds |
| Runtime heterogeneity | Choices |
| Expected failure | `Error!`, postfix propagation and `catch` |
| Platform integration | Builder packages, external packages and source-backed packages |
| Memory safety | GC baseline plus mandatory borrow validation |
| Mutation | Explicit exclusive access rather than implicit mutation |
| Ownership | Compiler-inferred transfer rather than source-visible move or lifetime syntax |
| Performance | Hidden compiler and backend optimisation |

## Major outside-scope families

The exact list lives in `docs/language-overview.md`. Major families include:

- general-purpose macros and second compile-time languages
- general closures, anonymous function values and broad higher-order polymorphism
- dynamic trait values and erased runtime dispatch
- broad trait or type-level programming systems
- structural, blanket, conditional, negative or specialized conformance
- operator overloading and general user-defined constraint systems
- reflection, runtime type inspection and type-returning functions
- higher-kinded types, partial type application and broad const generics
- user-extensible builtin hashmap key semantics
- public exceptions and competing broad result systems
- source-visible lifetime, reference-category and ownership annotation systems
- backend-specific semantics leaking into the language

## Review questions

- Does this add a new source-language category when an existing constrained
  mechanism is sufficient?
- Does it make behaviour more implicit?
- Does it need whole-program or solver-heavy reasoning?
- Does it introduce runtime type or erased-dispatch machinery?
- Does it leak backend representation into source semantics?
- Does it expose optimisation mechanics through source syntax?
- Is it merely unimplemented, or explicitly outside scope?
- Would the feature make code harder to inspect locally?

## Read next

- `docs/language-overview.md`
- `docs/src/docs/codebase/memory-management/overview.bd`
- `docs/compiler-design-overview.md`
- `docs/src/docs/codebase/language/overview.bd`
- `docs/src/docs/progress/#page.bst`
