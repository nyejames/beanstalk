//! Target path resolution for `bean new html`.
//!
//! WHAT: Will resolve user-provided paths into canonical project directories.
//! WHY: Path semantics (omitted, relative, absolute, home expansion) belong in one place.
//!
// TODO(Phase 3): Implement `ResolvedProjectTarget` and resolution logic here.
